# 基础环境约束
#pthon>=3.8,<3.9  # Devign 适配 3.8，避免 3.9+ 兼容性问题
# 核心数据处理库（版本严格匹配）
pandas>=1.0.1,<1.5  # 兼容 torch 1.4.0 的稳定版
scikit-learn>=0.22.2,<1.4
gensim>=3.8.1,<4.4
cpgclientlib>=0.11.111,<0.12
numpy>=1.19,<1.22  # 适配 torch 1.4.0 的 numpy 版本（1.22+ 不兼容）

# PyTorch 核心（指定 CPU 源，避免自动装 GPU 版）
torch==1.4.0+cpu
torchvision==0.5.0+cpu
# 若用 GPU（CUDA 10.1），替换为：
# torch==1.4.0+cu101
# torchvision==0.5.0+cu101

# PyTorch Geometric 核心依赖（版本严格匹配 torch 1.4.0）
torch-scatter==2.0.4
torch-sparse==0.6.1
torch-cluster==1.5.4
torch-spline-conv==1.2.0
torch-geometric>=1.4.2,<1.7  # 适配 torch 1.4.0 的最高稳定版

# 辅助依赖（显式标注避免隐式缺失）
tqdm>=4.45,<4.65
matplotlib>=3.1,<3.5
networkx>=2.4,<2.8
joblib>=0.14,<1.2  # scikit-learn 隐性依赖，显式装避免报错
pyparsing>=2.4,<3.1  # matplotlib 依赖


